# Branch Banker Dashboard

A simple React dashboard for branch bankers to review client financial information using simulated data.

## Setup

Install dependencies:

```bash
npm install
```

Run the app:

```bash
npm run dev
```

Open the local Vite URL shown in the terminal.
